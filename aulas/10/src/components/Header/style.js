import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    viewHeader: {
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 8,
        marginLeft: 10,
        width:'90%',
    },
    textHeader: {
        fontSize: 25,
        color: 'white',
        fontWeight: "bold",
        marginRight: 75,
        marginLeft: 60
    }
})