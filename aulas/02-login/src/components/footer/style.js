import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    boxComponent: {
        alignItems: 'center',
        justifyContent: 'center',
        padding: 10
    }, 

    textComponent: {
        color: 'gray',
        fontSize: 15,
        fontWeight: 'sm-bold'
    }, 

    messageComponent: {
        marginTop: 30,
        fontSize: 12,
        fontWeight: 'sm-bold',
        color: 'red'
    }
})