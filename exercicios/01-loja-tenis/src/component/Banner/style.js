import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    imageBanner: {
        width: '90%',
        height: 200,
        borderRadius: 10,
        marginTop: -30,
        marginHorizontal: '5%',
        marginBottom: '2%'
    },
    textBanner: {
        fontSize: 30,
        color: 'white',
        marginTop: 15,
        marginLeft: 10
    }
})
